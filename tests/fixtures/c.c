/*
 * A comment with FIXME DIRTY keywords
 * After this comment, there should be a trailing whitespace:
 */
	
#include <stdio.h>

#define ANIMAL "cat"

int main(int argc, char **argv)
{
   	something(); // before this, misused whitespaces should be highlighted

        spaced_indentation();

	return 0;
}
